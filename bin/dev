#!/usr/bin/env sh

# Default to port 3000 if not specified
export PORT="${PORT:-3000}"

# Use bundle exec to ensure foreman comes from the app's bundle
exec bundle exec foreman start -f Procfile.dev --env /dev/null "$@"

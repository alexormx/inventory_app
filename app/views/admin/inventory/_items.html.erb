<turbo-frame id="inventory-items-frame-<%= product.id %>" class="mt-2">
  <table class="table table-sm table-bordered mt-2">
    <thead>
      <tr>
        <th>ID</th>
        <th>Status</th>
        <th>Purchase Order</th>
        <th>Sale Order</th>
        <th>Purchase Cost</th>
        <th>Sale Price</th>
        <th>Updated</th>
      </tr>
    </thead>
    <tbody>
      <% items.each do |item| %>
        <tr>
          <td><%= item.id %></td>
          <td><span class="badge bg-<%= inventory_status_color(item.status) %>"><%= item.status.humanize %></span></td>
          <td><%= item.purchase_order_id || "—" %></td>
          <td><%= item.sale_order_id || "—" %></td>
          <td><%= number_to_currency(item.purchase_cost) %></td>
          <td><%= number_to_currency(item.sold_price) %></td>
          <td><%= item.status_changed_at&.strftime("%F") %></td>
        </tr>
      <% end %>
    </tbody>
  </table>
</turbo-frame>

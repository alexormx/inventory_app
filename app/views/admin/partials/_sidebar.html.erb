<div class="sidebar-inner vh-100">
  <div class="d-flex justify-content-between align-items-center mb-3">
    <h5 class="mb-0">
      <i class="fa-solid fa-gear sidebar-label"></i>
      <span class="sidebar-label">PASATIEMPOS</span>
    </h5>
  <button id="sidebarToggle" class="sidebar-toggle-btn d-md-inline d-none" aria-label="Toggle sidebar">
      <i class="fa-solid fa-bars"></i>
    </button>
  </div>

  <ul class="nav flex-column">
    <!-- 📊 Dashboard -->
    <li class="nav-item mb-1">
      <% active = request.path.start_with?(admin_dashboard_path) %>
      <%= link_to admin_dashboard_path, class: "nav-link text-white d-flex align-items-center#{active ? ' active' : ''}", aria: (active ? { current: 'page' } : {}) do %>
        <i class="fa-solid fa-chart-line me-2"></i>
        <span class="sidebar-label">Dashboard</span>
      <% end %>
    </li>

    <hr class="text-secondary my-2">

    <!-- 🧑 Users -->
    <li class="text-uppercase small text-secondary ps-2 mb-1 sidebar-label">Users</li>
    <li class="nav-item">
      <% active = request.path.start_with?(admin_users_path) %>
      <%= link_to admin_users_path, class: "nav-link text-white d-flex align-items-center#{active ? ' active' : ''}", aria: (active ? { current: 'page' } : {}) do %>
        <i class="fa fa-users me-2"></i>
        <span class="sidebar-label">Users</span>
      <% end %>
    </li>

    <hr class="text-secondary my-2">

    <!-- 📦 Inventory -->
    <li class="text-uppercase small text-secondary ps-2 mb-1 sidebar-label">Inventory</li>
    <li class="nav-item">
      <% active = request.path.start_with?(admin_products_path) %>
      <%= link_to admin_products_path, class: "nav-link text-white d-flex align-items-center#{active ? ' active' : ''}", aria: (active ? { current: 'page' } : {}) do %>
        <i class="fa fa-tags me-2"></i>
        <span class="sidebar-label">Products</span>
      <% end %>
    </li>
      <li class="nav-item">
        <% active = request.path.start_with?(admin_inventory_adjustments_path) %>
        <%= link_to admin_inventory_adjustments_path, class: "nav-link text-white d-flex align-items-center#{active ? ' active' : ''}", aria: (active ? { current: 'page' } : {}) do %>
          <i class="fa-solid fa-scale-balanced me-2"></i>
          <span class="sidebar-label">Inventory Adjustments</span>
        <% end %>
      </li>
    <li class="nav-item">
      <% active = request.path.start_with?(admin_purchase_orders_path) %>
      <%= link_to admin_purchase_orders_path, class: "nav-link text-white d-flex align-items-center#{active ? ' active' : ''}", aria: (active ? { current: 'page' } : {}) do %>
        <i class="fa fa-truck me-2"></i>
        <span class="sidebar-label">Purchase Orders</span>
      <% end %>
    </li>

    <li class="nav-item">
      <% active = request.path.start_with?(admin_sale_orders_path) %>
      <%= link_to admin_sale_orders_path, class: "nav-link text-white d-flex align-items-center#{active ? ' active' : ''}", aria: (active ? { current: 'page' } : {}) do %>
        <i class="fa fa-shopping-cart me-2"></i>
        <span class="sidebar-label">Sale Orders</span>
      <% end %>
    </li>
    <li class="nav-item">
      <% active = request.path.start_with?(admin_preorders_path) %>
      <%= link_to admin_preorders_path, class: "nav-link text-white d-flex align-items-center#{active ? ' active' : ''}", aria: (active ? { current: 'page' } : {}) do %>
        <i class="fa fa-hourglass-half me-2"></i>
        <span class="sidebar-label">Preórdenes / Sobre pedido</span>
      <% end %>
    </li>
    <li class="nav-item">
      <% active = request.path.start_with?(admin_inventory_path) %>
      <%= link_to admin_inventory_path, class: "nav-link text-white d-flex align-items-center#{active ? ' active' : ''}", aria: (active ? { current: 'page' } : {}) do %>
        <i class="fa fa-archive me-2"></i>
        <span class="sidebar-label">Inventory</span>
      <% end %>
    </li>

    <hr class="text-secondary my-2">

    <!-- ⚙️ System -->
    <li class="text-uppercase small text-secondary ps-2 mb-1 sidebar-label">System</li>
    <li class="nav-item">
      <% active = request.path.start_with?(admin_reports_path) %>
      <%= link_to admin_reports_path, class: "nav-link text-white d-flex align-items-center#{active ? ' active' : ''}", aria: (active ? { current: 'page' } : {}) do %>
        <i class="fa-solid fa-chart-pie me-2"></i>
        <span class="sidebar-label">Reports</span>
      <% end %>
    </li>
    <li class="nav-item">
      <% active = request.path.start_with?(admin_settings_path) %>
      <%= link_to admin_settings_path, class: "nav-link text-white d-flex align-items-center#{active ? ' active' : ''}", aria: (active ? { current: 'page' } : {}) do %>
        <i class="fa-solid fa-sliders me-2"></i>
        <span class="sidebar-label">Settings</span>
      <% end %>
    </li>
    <li class="nav-item">
      <% active = request.path.start_with?(admin_system_variables_path) %>
      <%= link_to admin_system_variables_path, class: "nav-link text-white d-flex align-items-center#{active ? ' active' : ''}", aria: (active ? { current: 'page' } : {}) do %>
        <i class="fa-solid fa-database me-2"></i>
        <span class="sidebar-label">System Variables</span>
      <% end %>
    </li>
    <li class="nav-item">
      <% active = request.path.start_with?(admin_visitor_logs_path) %>
      <%= link_to admin_visitor_logs_path, class: "nav-link text-white d-flex align-items-center#{active ? ' active' : ''}", aria: (active ? { current: 'page' } : {}) do %>
        <i class="fa-solid fa-eye me-2"></i>
        <span class="sidebar-label">Visitor Logs</span>
      <% end %>
    </li>
  </ul>
</div>
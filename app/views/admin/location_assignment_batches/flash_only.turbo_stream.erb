<%# El add falló: no se toca NADA de la pantalla. El término, sus resultados, la
    cantidad tecleada, la ubicación y el lote se quedan como están; sólo aparece
    el aviso para que el operador corrija el dato que falló. %>
<%= render 'flash_streams' %>

<!DOCTYPE html>
<html lang="en">
<head>
  <title><%= content_for(:title) || 'Inventory App' %></title>
  <meta name="description" content="<%= content_for(:description) || 'Explore our products' %>">
  <link rel="canonical" href="<%= request.original_url %>">
  <meta property="og:title" content="<%= content_for(:title) || 'Inventory App' %>">
  <meta property="og:description" content="<%= content_for(:description) || 'Explore our products' %>">
  <meta property="og:url" content="<%= request.original_url %>">
  <meta property="og:image" content="<%= image_url('logo.png') %>">
  <meta name="twitter:card" content="summary_large_image">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <meta charset="utf-8">
  <%= csrf_meta_tags %>
  <%= csp_meta_tag %>

  <!-- ✅ FontAwesome via CDN -->
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.1/css/all.min.css">

    <%= favicon_link_tag 'favicon.ico' %>
    <%= favicon_link_tag 'favicon.svg', type: 'image/svg+xml' %>
    <link rel="apple-touch-icon" href="/favicon.ico">
  
  <%= stylesheet_link_tag "application", media: "all", "data-turbo-track": "reload" %>
  <%= javascript_importmap_tags %>
</head>

<div id="cookie-banner" class="cookie-banner bg-dark text-white p-3 fixed-bottom d-none">
  <div class="container d-flex justify-content-between align-items-center">
    <div>
      Este sitio utiliza cookies y tecnologías similares para ofrecerte una mejor experiencia. Al continuar, aceptas el uso de cookies y almacenamiento de sesión.
    </div>
    <button id="accept-cookies" class="btn btn-light btn-sm">Aceptar</button>
  </div>
</div>

<body>

  <!-- ✅ NAVBAR -->
  <header>
    <%= render 'customers/partials/navbar' %>
  </header>

  <!-- ✅ Main Content -->
    <main class="container mt-4">
      <%= render 'layouts/flash' %>
      <%= yield %>
    </main>

  <!-- ✅ Footer -->
  <%= render 'customers/partials/footer' %>

</body>
</html>
